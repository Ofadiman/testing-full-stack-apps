{
  "name": "@cypressing-react/api",
  "type": "module",
  "main": "src/main.ts",
  "scripts": {
    "dev": "tsup",
    "format": "prettier --write .",
    "types": "tsc --noEmit",
    "migrations:generate": "drizzle-kit generate",
    "migrations:migrate": "drizzle-kit migrate"
  },
  "volta": {
    "extends": "../../package.json"
  }
}
