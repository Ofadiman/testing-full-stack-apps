{
  "name": "@cypressing-react/api",
  "type": "module",
  "main": "src/main.ts",
  "scripts": {
    "dev": "tsup",
    "format": "prettier --write .",
    "types": "tsc --noEmit",
    "migrations:generate": "docker compose exec --workdir /home/developer/cypressing-react/apps/api api pnpm drizzle-kit generate",
    "migrations:migrate": "docker compose exec --workdir /home/developer/cypressing-react/apps/api api pnpm drizzle-kit migrate"
  },
  "volta": {
    "extends": "../../package.json"
  }
}
